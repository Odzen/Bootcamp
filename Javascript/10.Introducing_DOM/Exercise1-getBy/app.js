/*
Write code in app.js to select the fllowing elements:
    - Image element by its id and save it to a variable called image
    - Select h1 by its id and save it to a variable called heading
*/

const image = document.getElementById("unicorn");
const heading = document.getElementById("mainheading");

console.dir(image);
console.dir(heading);